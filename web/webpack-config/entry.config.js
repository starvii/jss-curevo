const path = require('path');
const dirVars = require('./base/dir-vars.config');
const configEntry = {
    index: dirVars.srcRootDir + '/js/index',
};


exports = module.exports = configEntry;
